on: push
name: 'Shellcheck'
jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Shellcheck
      uses: iamjohnnnym/action-shellcheck@0.1.1rc

